Letters-only copy of the live `last-dletter` server dictionary (`data/words.txt`)
and the exact required-prefix blacklist from `lib/prefixBlacklist.js`.

Hyphens and apostrophes exist in the original list; the fly keyboard is A–Z
only, so training loads `letters_only=True` by default.
